# دليل إصلاح النصوص العربية 🛠️

## المشكلة
معظم أدوات الذكاء الاصطناعي (مثل Nano Banana, Midjourney, Stable Diffusion) لا تدعم الكتابة باللغة العربية بشكل صحيح، مما يؤدي إلى ظهور حروف مقطعة أو معكوسة.

## الحل
لقد قمنا بتوفير حل برمجي بسيط يضمن ظهور النصوص العربية بشكل مثالي.

### الخطوات:

1. **تثبيت المتطلبات**:
   افتح التيرمينال ونفذ الأمر التالي:
   ```bash
   pip install -r requirements.txt
   ```

2. **توليد الصورة**:
   - افتح ملف `ads_tiktok_fixed.yaml`.
   - انسخ الـ Prompt الخاص بالمدينة (مثلاً الرياض).
   - الصقه في Nano Banana وولد الصورة.
   - احفظ الصورة باسم (مثلاً `riyadh.png`).

3. **إضافة النص العربي**:
   استخدم السكريبت المرفق `add_text.py` لإضافة النصوص:
   ```bash
   python add_text.py riyadh.png الرياض
   ```
   
   سيقوم السكريبت بإنشاء ملف جديد `fixed_riyadh.png` يحتوي على النصوص العربية الصحيحة (العنوان والرسائل).

## لماذا نجح الأمر سابقاً؟
إذا رأيت صورة بنصوص عربية صحيحة سابقاً، فغالباً تم استخدام:
1. نموذج متطور جداً (مثل DALL-E 3) الذي يدعم العربية جزئياً (ولكنه يخطئ أحياناً).
2. أو تم استخدام تقنية "In-painting" أو تعديل يدوي.
3. الحل الذي نقدمه هنا هو الأكثر ضماناً واحترافية.

## ملاحظات
- السكريبت يستخدم خطوط النظام (Arial أو Segoe UI).
- يمكنك تعديل السكريبت `add_text.py` لتغيير حجم الخط أو مكانه.
